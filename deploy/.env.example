# ==========================================
# ChatBI 部署配置
# 复制为 deploy/.env 并修改实际值
# ==========================================

# ---- 暴露端口 ----
CHATBI_PORT=8080

# ---- MySQL（仅使用 docker-compose with-mysql profile 时需要） ----
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=yjt_mysql
MYSQL_DATABASE=chatbi

# ---- Redis（仅使用 docker-compose with-redis profile 时需要） ----
REDIS_PORT=6379

# ---- 后端必需密钥（首次部署留空自动生成） ----
SECRET_KEY=
DATA_SOURCE_ENCRYPTION_KEY=

# ---- 数据库连接 ----
# 使用外部 MySQL（IP 模式，推荐）：
DATABASE_URL=mysql+aiomysql://root:yjt_mysql@192.168.3.110:3306/chatbi
# 使用 docker-compose 内置 MySQL（需启用 with-mysql profile）：
# DATABASE_URL=mysql+aiomysql://root:yjt_mysql@mysql:3306/chatbi

# ---- Redis ----
# 使用外部 Redis（IP 模式）：
REDIS_URL=redis://192.168.3.110:6379/0
# 使用 docker-compose 内置 Redis（需启用 with-redis profile）：
# REDIS_URL=redis://redis:6379/0

# ---- LLM 配置（百炼 API） ----
LLM_BASE_URL=https://coding.dashscope.aliyuncs.com/v1
LLM_API_KEY=sk-sp-494c9e75802b4f0c9422b79d7c4f8006
LLM_MODEL=qwen3.6-plus

# ---- CORS（生产环境填实际域名，如 ["https://chatbi.example.com"]） ----
CORS_ORIGINS=["*"]

# ---- 前端 URL（邮件验证链接等） ----
FRONTEND_URL=https://chatbi.example.com

# ---- SMTP（邮箱验证 / 密码重置，留空则跳过） ----
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# ---- 日志 ----
LOG_LEVEL=INFO
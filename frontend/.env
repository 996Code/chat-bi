# Frontend build environment variables
# VITE_ prefix required — only these are exposed to client-side code

# App base path (Vite builds assets with this prefix)
VITE_BASE_PATH=/chat-bi/

# API prefix (must match backend API_PREFIX)
VITE_API_PREFIX=/chat-bi/api/v1

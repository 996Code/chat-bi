# Missing Test Cases

## security.py (app/core/security.py)

1. **test_verify_access_token_with_refresh_token** — `verify_access_token` should return `None` when given a refresh token (type mismatch: `expected_type="access"` vs `type="refresh"`)
2. **test_verify_refresh_token_with_access_token** — `verify_refresh_token` should return `None` when given an access token (type mismatch: `expected_type="refresh"` vs `type="access"`)
3. **test_get_current_user_malformed_bearer_extra_space** — `Authorization: Bearer  token` (double space) should return 401 with INVALID_TOKEN
4. **test_get_current_user_bearer_only_no_token** — `Authorization: Bearer ` (empty after Bearer) should return 401 with INVALID_TOKEN
5. **test_get_current_user_empty_bearer** — `Authorization: Bearer` (no space after Bearer) should return 401 with UNAUTHORIZED (since `startswith("Bearer ")` requires trailing space)
6. **test_create_access_token_custom_expiry** — `create_access_token` with `expires_delta` parameter should produce a token with the correct exp
7. **test_create_access_token_empty_data** — `create_access_token` with empty dict `{}` should still produce a valid token
8. **test_hash_password_empty_string** — `hash_password("")` should not raise and produce a valid bcrypt hash
9. **test_verify_password_wrong_password** — `verify_password("wrong", correct_hash)` should return `False`
10. **test_verify_password_empty_hash** — `verify_password("pass", "")` should return `False` (bcrypt handles invalid hash)
11. **test_password_reset_token_verify_with_expired_token** — `verify_password_reset_token` with a token whose `max_age=1800` has passed should return `None`
12. **test_password_reset_token_verify_tampered** — `verify_password_reset_token` with a modified/corrupted token should return `None`
13. **test_password_reset_token_verify_non_email_payload** — `verify_password_reset_token` for a token with no colon (no nonce) should still return the payload (backward compat path)
14. **test_email_verification_token_expired** — `verify_email_verification_token` with expired token (>24h) should return `None`
15. **test_email_verification_token_tampered** — `verify_email_verification_token` with tampered token should return `None`
16. **test_get_current_user_with_refresh_token** — Using a refresh token for `get_current_user` should return 401 (token type mismatch)
17. **test_algorithm_confusion_attack** — Token signed with a different algorithm (e.g., RS256) should be rejected

## rate_limiter.py (app/core/rate_limiter.py)

18. **test_rate_limit_exactly_at_boundary** — Exactly `max_requests` requests should succeed (the Nth request is the last one allowed)
19. **test_rate_limit_one_over_boundary** — `max_requests + 1` requests should trigger 429 on the last one
20. **test_rate_limit_different_ips_independent** — IP A at rate limit should not affect IP B's requests to the same path
21. **test_rate_limit_same_ip_different_paths** — IP A rate-limited on `/api/v1/query` should still access `/api/v1/datasources`
22. **test_rate_limit_window_expiry** — After `window_seconds` passes, requests should be allowed again (clean_expired logic)
23. **test_rate_limit_auth_endpoint_has_lower_limit** — Login endpoint should have limit of 10/min (not the default 60)
24. **test_rate_limit_query_endpoint_has_custom_limit** — Query endpoint should have limit of 30/min
25. **test_rate_limit_health_check_exempt** — Requests to `/health` should bypass rate limiting regardless of count
26. **test_rate_limit_docs_endpoint_exempt** — Requests to `/docs` and `/openapi.json` should bypass rate limiting
27. **test_rate_limit_missing_client_ip** — When `request.client` is `None`, should fall back to `"unknown"` key
28. **test_rate_limit_rapid_burst_same_second** — 11 rapid login requests within the same second should trigger lock on the 11th
29. **test_rate_limit_clean_expired_removes_old** — `_clean_expired` should only remove timestamps older than the cutoff, not all

## auth.py (app/api/auth.py)

30. **test_login_case_insensitive_email** — Login with `"Test@Example.com"` should match user registered with `"test@example.com"` (query uses `User.email == req.email` which is case-sensitive; this is a potential bug worth testing)
31. **test_login_locked_then_expiry_allows** — After 5 failures lock the account, simulate lock expiry (set `locked_until` to past), then login should succeed
32. **test_login_failure_count_persisted_across_attempts** — After 3 failures, failed_login_attempts should be 3 (verify DB state)
33. **test_password_reset_confirm_invalid_token** — POST `/reset-password/confirm` with invalid/tampered token should return 400
34. **test_password_reset_confirm_expired_token** — POST `/reset-password/confirm` with expired token (>30min) should return 400
35. **test_password_reset_confirm_user_not_found** — Token for email that no longer exists in DB should return 400
36. **test_password_reset_confirm_new_password_too_short** — New password < 8 chars should fail Pydantic validation (422)
37. **test_password_reset_confirm_success_resets_failed_attempts** — After successful reset, `failed_login_attempts` should be 0
38. **test_password_reset_confirm_new_password_no_letter** — Password without letters should fail validation
39. **test_password_reset_confirm_new_password_no_number** — Password without numbers should fail validation
40. **test_password_reset_confirm_same_as_old_password** — Should still accept (no "must be different" check exists)
41. **test_verify_email_invalid_token** — POST `/verify-email` with invalid token should return 400
42. **test_verify_email_expired_token** — POST `/verify-email` with expired token (>24h) should return 400
43. **test_verify_email_already_verified** — Verifying an already-verified email should still return 200 (idempotent)
44. **test_register_whitespace_email** — Registration with email containing spaces should be rejected by EmailStr validator
45. **test_register_password_exactly_8_chars** — Password of exactly 8 chars meeting all criteria should be accepted
46. **test_register_password_exactly_7_chars** — Password of 7 chars should be rejected (422)
47. **test_register_password_only_special_chars** — Password like `!@#$%^&*` (no letters, no digits) should be rejected
48. **test_register_password_unicode** — Password with Unicode characters should work if it meets letter/digit requirements
49. **test_login_success_resets_failed_attempts** — Successful login should reset `failed_login_attempts` to 0 in DB
50. **test_refresh_token_missing_user_id_claim** — Refresh token with missing `user_id` should still generate new access token (uses `.get()` defaulting to None)
51. **test_refresh_token_with_type_access** — Using an access token as refresh token should return 401 (type mismatch)
52. **test_reset_password_timing_no_leak** — Reset for existing vs non-existing user should take similar time (constant-time response)

## datasource.py (app/api/datasource.py)

53. **test_scan_datasource_connection_fails** — POST `/{ds_id}/scan` when connection test fails should return 400 CONNECTION_FAILED
54. **test_update_datasource_credentials_refreshes_pool** — Updating username/password should trigger `pool_manager.refresh_pool`
55. **test_get_datasource_schema_invalid_json** — When metadata config contains invalid JSON, `json.loads` should raise (500 error path exists)
56. **test_datasource_name_exactly_100_chars** — Name of exactly 100 chars should be accepted
57. **test_datasource_name_101_chars** — Name of 101 chars should be rejected (422)
58. **test_create_datasource_unsupported_type_oracle** — `type: "oracle"` should be rejected (422)
59. **test_create_datasource_unsupported_type_mongodb** — `type: "mongodb"` should be rejected (422)
60. **test_create_datasource_sqlite_type_rejected** — `type: "sqlite"` should be rejected (not in supported list, even though connection_pool supports it)
61. **test_update_datasource_partial_update_host_only** — Update only host should leave other fields unchanged
62. **test_update_datasource_tenant_isolation** — User A cannot update User B's datasource (should return 404)
63. **test_delete_datasource_tenant_isolation** — User A cannot delete User B's datasource (should return 404)
64. **test_test_datasource_tenant_isolation** — User A cannot test User B's datasource
65. **test_scan_datasource_tenant_isolation** — User A cannot scan User B's datasource
66. **test_health_check_updates_is_active** — After health check, `is_active` should reflect result (True for healthy, False for unhealthy)
67. **test_datasource_name_whitespace_only** — Name with only spaces should be rejected by `name_not_empty` validator
68. **test_create_datasource_unicode_name** — Datasource with Unicode name (中文) should work
69. **test_get_datasource_schema_tenant_isolation** — User A cannot get User B's datasource schema
70. **test_create_datasource_extra_params_ignored** — `extra_params` in DataSourceCreate should be stored but not used (no test verifies this)
71. **test_datasource_response_does_not_leak_credentials** — Response should not contain username/password fields (partially tested in e2e)

## query.py (app/api/query.py)

72. **test_create_query_cross_tenant_datasource** — User A querying User B's datasource_id should return 404 (DATASOURCE_NOT_FOUND)
73. **test_create_query_llm_returns_empty_sql** — When graph returns empty SQL, response should have `success=False`
74. **test_create_query_no_metadata_config** — Query without metadata config (no scan done) should still work with empty schema_context
75. **test_create_query_unicode_question** — Question with full Unicode (Chinese, emojis, etc.) should work
76. **test_create_query_question_near_max_length** — Question at 999 chars should work; 1000+ should be rejected
77. **test_create_query_question_with_sql_keywords** — Question like "SELECT all users" should be classified and not cause issues
78. **test_create_query_question_whitespace_only** — Whitespace-only question should be rejected by QueryRequest validator (422)
79. **test_stream_query_non_dataquery_intent** — Stream query with greeting question should return intent=Other and `success: false`
80. **test_stream_query_no_metadata_config** — Stream query without metadata config should work with empty schema_context
81. **test_stream_query_unicode_question** — Stream query with Unicode question should work
82. **test_stream_query_cross_tenant_datasource** — Stream query with another tenant's datasource_id should return 404
83. **test_stream_query_llm_returns_empty_sql** — When generate_sql returns empty, should yield error event
84. **test_stream_query_self_heal_triggered** — Stream query where first execution fails should trigger self-heal
85. **test_create_query_response_structure** — Verify QueryResponse has all required fields with correct types

## saved_query.py (app/api/saved_query.py)

86. **test_save_query_whitespace_name** — Name with only spaces (e.g., `"   "`) should be rejected (400 INVALID_INPUT after `.strip()`)
87. **test_save_query_no_datasource_id** — Save without `datasource_id` should return 400
88. **test_save_query_null_datasource_id** — Save with `datasource_id: null` should be rejected (400)
89. **test_list_queries_page_beyond_results** — `page=999` should return empty list
90. **test_list_queries_page_size_max** — `page_size=200` (above le=100 limit) should return 422
91. **test_list_queries_page_zero** — `page=0` should return 422 (ge=1)
92. **test_get_query_other_users_query** — GET `/queries/{id}` for another user's query should return 404
93. **test_get_query_invalid_uuid_format** — GET `/queries/not-a-uuid` should return appropriate error
94. **test_delete_query_other_users_query** — DELETE `/queries/{id}` for another user's query should return 404
95. **test_delete_query_invalid_uuid** — DELETE with invalid UUID format should return error
96. **test_rerun_query_other_users_query** — POST `/{query_id}/re-run` for another user's query should return 404
97. **test_rerun_query_deleted_query** — Re-run a deleted query should return 404
98. **test_save_query_unicode_name_and_text** — Unicode in name, query_text, and generated_sql should work
99. **test_save_query_max_length_name** — Name at 200 chars (model limit) should work; 201 should fail
100. **test_list_queries_empty_list** — New user with no saved queries should get empty list

## export.py (app/api/export.py)

101. **test_csv_injection_equals_prefix** — Cell value `"=SUM(A1:A10)"` should be prefixed with `'` to prevent formula injection
102. **test_csv_injection_plus_prefix** — Cell value `"+SUM(A1:A10)"` should be prefixed with `'`
103. **test_csv_injection_minus_prefix** — Cell value `"-SUM(A1:A10)"` should be prefixed with `'`
104. **test_csv_injection_at_prefix** — Cell value `"@SUM(A1:A10)"` should be prefixed with `'`
105. **test_csv_injection_tab_prefix_bypass** — Cell value `"\t=SUM(A1:A10)"` (tab before equals) — should still be sanitized
106. **test_csv_cell_with_comma** — Cell value containing comma should be properly quoted in CSV output
107. **test_csv_cell_with_newline** — Cell value containing newline should be properly escaped in CSV output
108. **test_csv_cell_with_double_quote** — Cell value containing `"` should be properly escaped (doubled) in CSV
109. **test_csv_rows_missing_column** — Row dict missing a column key should produce empty string (not KeyError)
110. **test_csv_rows_with_extra_columns** — Row dict with keys not in columns list should ignore extras
111. **test_csv_large_dataset** — Export with 1000+ rows should complete without error
112. **test_csv_unicode_in_values** — Unicode characters (Chinese, emoji) in cell values should round-trip correctly
113. **test_csv_non_string_values** — Integer, float, boolean values in cells should be serialized correctly
114. **test_csv_columns_only_no_rows** — `columns: ["name"], rows: []` should return 404 (NO_DATA)
115. **test_csv_rows_only_no_columns** — `columns: [], rows: [{"a": 1}]` should return 404 (NO_DATA)
116. **test_csv_content_disposition_header** — Response should include `Content-Disposition: attachment` header

## audit.py (app/api/audit.py)

117. **test_list_audit_logs_admin_success** — Admin user should see audit logs for their tenant
118. **test_list_audit_logs_filter_by_action** — `?action=USER_LOGIN` should filter logs by action
119. **test_list_audit_logs_filter_nonexistent_action** — `?action=NONEXISTENT` should return empty list
120. **test_list_audit_logs_pagination** — `page=1&page_size=2` should return at most 2 logs
121. **test_list_audit_logs_page_beyond** — `page=999` should return empty list
122. **test_list_audit_logs_empty_tenant** — Admin with no audit logs should get empty list
123. **test_list_audit_logs_tenant_isolation** — Admin A should not see Admin B's tenant logs
124. **test_list_audit_logs_user_not_admin** — Non-admin user should get 403 (already tested as test_access_without_token in e2e_comprehensive.py step 10)
125. **test_list_audit_logs_page_size_boundary** — `page_size=201` (above le=200) should return 422

## feedback.py (app/api/feedback.py)

126. **test_feedback_no_comment_field** — Feedback without `comment` should default to empty string
127. **test_feedback_no_query_id_field** — Feedback without `query_id` should default to empty string
128. **test_feedback_unicode_comment** — Comment with Unicode (Chinese, emoji) should be stored correctly
129. **test_feedback_special_chars_comment** — Comment with SQL-like content should be stored as-is (no injection)
130. **test_feedback_empty_body** — POST with `{}` should return 400 (rating not in ("up", "down"))
131. **test_feedback_rating_case_sensitive** — Rating `"UP"` (uppercase) should be rejected (400)
132. **test_feedback_list_pagination** — `list_feedback` with `page` and `page_size` params should paginate
133. **test_feedback_list_page_size_boundary** — `page_size=201` (above le=200) should return 422
134. **test_feedback_list_page_zero** — `page=0` should return 422 (ge=1)
135. **test_feedback_comment_max_length** — Comment at 1000 chars (model limit) should work; 1001 should fail
136. **test_feedback_same_user_multiple** — Same user can submit multiple feedbacks (no uniqueness constraint)

## graph.py (app/ai/graph.py)

137. **test_route_by_intent_dataquery** — `route_by_intent` with `intent="DataQuery"` should return `"generate_sql"`
138. **test_route_by_intent_other** — `route_by_intent` with `intent="Other"` should return `"misleading"`
139. **test_route_by_intent_missing_intent** — `route_by_intent` with no intent key should return `"misleading"` (`.get()` returns None)
140. **test_route_by_intent_empty_string** — `route_by_intent` with `intent=""` should return `"misleading"`
141. **test_handle_misleading_sets_error** — `handle_misleading` should set `success=False` and an error message
142. **test_handle_misleading_preserves_state** — `handle_misleading` should preserve other state fields (question, etc.)
143. **test_build_graph_returns_compiled_graph** — `build_graph()` should return a compiled LangGraph instance
144. **test_build_graph_singleton** — Multiple calls to `build_graph()` should return the same instance
145. **test_graph_with_empty_question** — Graph ainvoke with empty question should handle gracefully

## intent.py (app/ai/nodes/intent.py)

146. **test_classify_intent_greeting_nihao** — `"你好"` should return `"Other"` (heuristic shortcut)
147. **test_classify_intent_greeting_hello** — `"hello"` should return `"Other"`
148. **test_classify_intent_greeting_thanks** — `"谢谢"` and `"好"` should return `"Other"`
149. **test_classify_intent_dataquery_keywords** — Questions with "查询", "统计", "SQL", "数据" should return `"DataQuery"` (via LLM or fallback)
150. **test_classify_intent_whitespace_only** — Whitespace-only question should not match heuristic shortcuts (falls through to LLM)
151. **test_classify_intent_very_long_question** — Very long question (>1000 chars) should not crash
152. **test_classify_intent_mixed_language** — Question mixing Chinese and English should work
153. **test_classify_intent_question_mark** — `"什么是销售额？"` should be classified as DataQuery
154. **test_intent_llm_fallback_query_keywords** — When LLM returns unexpected text containing query indicators, should fallback to `"DataQuery"`
155. **test_intent_llm_fallback_non_query** — When LLM returns unexpected text without query indicators, should fallback to `"Other"`

## generation.py (app/ai/nodes/generation.py)

156. **test_generate_sql_with_schema_context** — `generate_sql` with non-empty schema_context should include it in prompt
157. **test_generate_sql_empty_schema_context** — `generate_sql` with empty schema_context should still work
158. **test_generate_sql_strips_markdown_code_block** — If LLM returns ````sql\nSELECT 1;\n``` ```, should strip to `SELECT 1;`
159. **test_generate_sql_strips_markdown_no_lang** — If LLM returns ````\nSELECT 1;\n``` ```, should strip to `SELECT 1;`
160. **test_generate_sql_no_markdown** — If LLM returns plain SQL, it should be returned as-is
161. **test_generate_sql_question_with_special_chars** — Question with quotes, backticks, etc. should work
162. **test_get_llm_singleton** — Multiple calls to `get_llm()` should return the same instance

## execution.py (app/ai/nodes/execution.py)

163. **test_validate_sql_real_function_with_parse_error** — `validate_sql("INVALID SQL")` should return `(False, "SQL 语法错误: ...")`
164. **test_validate_sql_call_stored_procedure** — `validate_sql("CALL my_proc()")` should be rejected (not Select)
165. **test_validate_sql_empty_string** — `validate_sql("")` should return `(False, ...)` (parse error)
166. **test_validate_sql_postgresql_dialect** — `validate_sql` with `dialect="postgresql"` should parse PG-specific syntax
167. **test_validate_sql_set_session_readonly** — `validate_sql("SET SESSION ...")` should be rejected (not Select)
168. **test_validate_sql_show_tables** — `validate_sql("SHOW TABLES")` should be rejected (not Select)
169. **test_validate_sql_select_with_stored_procedure_call** — `validate_sql("SELECT * FROM users WHERE fn(id)")` should be accepted
170. **test_execute_sql_pool_not_initialized** — `execute_sql` with unknown datasource_id should return `{"success": False, "error": "数据源连接池未初始化"}`
171. **test_execute_sql_truncation** — Query returning >1000 rows should truncate to MAX_ROWS with `truncated=True`
172. **test_execute_sql_empty_result** — Query with no rows should return `success=True` with empty columns and rows
173. **test_execute_sql_bytes_column** — Column with bytes value should be decoded to UTF-8 with error replacement
174. **test_execute_sql_datetime_column** — Column with datetime value should be converted to string
175. **test_execute_sql_none_values_in_rows** — Rows with None values should be handled (dict with None values)

## self_heal.py (app/ai/nodes/self_heal.py)

176. **test_extract_error_code_mysql_format** — `extract_error_code("(1146) Table doesn't exist")` should return `"1146"`
177. **test_extract_error_code_no_code** — `extract_error_code("Unknown error")` should return `None`
178. **test_extract_error_code_multiple_parens** — `extract_error_code("Error (1054) in (SELECT)")` should return first match
179. **test_extract_error_code_empty_string** — `extract_error_code("")` should return `None`
180. **test_build_fix_prompt_retry_message** — `build_fix_prompt` with `retry_count=2` should include "这是第 2 次重试"
181. **test_build_fix_prompt_no_retry_message** — `build_fix_prompt` with `retry_count=1` should NOT include retry message
182. **test_build_fix_prompt_with_schema_context** — `build_fix_prompt` with non-empty schema_context should include it
183. **test_build_fix_prompt_empty_schema_context** — `build_fix_prompt` with empty schema_context should omit "可用的表结构" section
184. **test_build_fix_prompt_known_error_code** — Error code 1146 should map to "表不存在" description
185. **test_build_fix_prompt_unknown_error_code** — Unknown error code should use first 200 chars of error message
186. **test_self_heal_llm_returns_empty** — When LLM returns empty SQL, should skip to next attempt
187. **test_self_heal_validation_fails** — When LLM generates invalid SQL (fails validate_sql), should continue to next attempt
188. **test_self_heal_exceeds_max_retries** — After MAX_RETRY (2) attempts still failing, should return failure message
189. **test_self_heal_empty_error_message** — `self_heal_sql` with empty error string should still attempt fix
190. **test_self_heal_llm_exception** — When LLM call raises exception, should catch and continue

## chart_type.py (app/ai/chart_type.py)

191. **test_infer_chart_pie_exactly_5_rows** — 2 columns, 5 rows, numeric second col → `"pie"` (boundary: `row_count <= 5`)
192. **test_infer_chart_pie_6_rows_not_pie** — 2 columns, 6 rows, numeric second col → `"bar"` (not pie, >5 rows)
193. **test_infer_chart_grouped_bar_3_cols_numeric_third** — 3 columns with numeric third → `"grouped_bar"`
194. **test_infer_chart_grouped_bar_non_numeric_third** — 3 columns with non-numeric third → `"table"`
195. **test_infer_chart_scatter_many_numeric_cols** — 4+ columns with 2+ numeric, 10+ rows → `"scatter"`
196. **test_infer_chart_scatter_not_enough_rows** — 2 numeric cols but <10 rows → not scatter
197. **test_infer_chart_fallback_table** — Data not matching any pattern → `"table"`
198. **test_looks_like_time_date_string** — `_looks_like_time("2024-01-15")` should return True
199. **test_looks_like_time_not_time** — `_looks_like_time("random_text")` should return False
200. **test_looks_like_time_none** — `_looks_like_time(None)` should return False
201. **test_is_numeric_string_number** — `_is_numeric("123.45")` should return True
202. **test_is_numeric_string_not_number** — `_is_numeric("hello")` should return False
203. **test_is_numeric_boolean** — `_is_numeric(True)` should return True (bool is subclass of int in Python)
204. **test_is_numeric_none** — `_is_numeric(None)` should return False
205. **test_sample_value_missing_column** — `_sample_value([{"a": 1}], "b")` should return None
206. **test_sample_value_empty_rows** — `_sample_value([], "a")` should return None
207. **test_is_numeric_column_all_none** — `_is_numeric_column("col", [{"col": None}])` should return False
208. **test_is_numeric_column_mixed_values** — `_is_numeric_column` with 2 numeric + 1 None out of 3 → True (>50%)

## models.py (app/db/models.py)

209. **test_tenant_default_uuid** — New Tenant should have auto-generated UUID primary key
210. **test_user_defaults** — New User should have `is_active=True`, `email_verified=False`, `role="user"`, `failed_login_attempts=0`, `is_locked=False`
211. **test_datasource_defaults** — New DataSource should have `db_type="mysql"`, `port=3306`, `is_active=True`
212. **test_datasource_required_fields** — DataSource without required fields (host, database_name, etc.) should fail
213. **test_feedback_default_comment** — New Feedback should have `comment=""` (default)
214. **test_analytics_event_default_data** — New AnalyticsEvent should have `event_data=""` (default)
215. **test_saved_query_fields** — SavedQuery should store all fields correctly
216. **test_audit_log_fields** — AuditLog should store all fields correctly
217. **test_metadata_config_fields** — MetadataConfig should store config as string (JSON)
218. **test_utcnow_precision** — `_utcnow()` should return datetime with microsecond precision (avoids SQLite 1s limit)

## connection_pool.py (app/services/connection_pool.py)

219. **test_build_url_mysql_format** — `_build_url` for MySQL datasource should produce `mysql+aiomysql://...` URL with `charset=utf8mb4`
220. **test_build_url_postgresql_format** — `_build_url` for PostgreSQL datasource should produce `postgresql+asyncpg://...` URL
221. **test_build_url_sqlite_format** — `_build_url` for SQLite datasource should produce `sqlite+aiosqlite://...` URL (no credentials)
222. **test_build_url_special_chars_in_credentials** — `_build_url` should properly URL-encode special characters in username/password (via `quote_plus`)
223. **test_get_pool_creates_new_engine** — `get_pool` for unknown datasource should create new AsyncEngine and store it
224. **test_get_pool_reuses_existing** — `get_pool` for known datasource should return existing engine (not create new)
225. **test_refresh_pool_disposes_old** — `refresh_pool` should dispose old pool before creating new one
226. **test_close_all_disposes_all** — `close_all` should dispose all engines and clear the pool dict
227. **test_get_pool_by_id_unknown** — `get_pool_by_id` for unknown ID should return `None`
228. **test_health_check_success** — `health_check` with working connection should return `{"healthy": True, "error": None}`
229. **test_health_check_failure** — `health_check` with broken connection should return `{"healthy": False, "error": "连接失败"}`

## datasource_service.py (app/services/datasource_service.py)

230. **test_list_all_tenant_isolation** — `list_all()` should only return datasources for the service's tenant_id
231. **test_get_by_id_invalid_uuid** — `get_by_id("not-a-uuid")` should raise ValueError (uuid.UUID constructor)
232. **test_update_no_fields** — `update` with empty DataSourceUpdate should return the datasource unchanged
233. **test_update_credentials_triggers_pool_refresh** — Updating username/password should call `pool_manager.refresh_pool`
234. **test_update_non_credentials_no_pool_refresh** — Updating only name should NOT trigger pool refresh
235. **test_delete_nonexistent_returns_false** — `delete` for non-existent datasource should return `False`
236. **test_test_connection_nonexistent** — `test_connection` for non-existent datasource should return `{"success": False, "error": "数据源不存在"}`
237. **test_connection_test_failure_sets_inactive** — Failed connection test should set `is_active=False` and commit
238. **test_connection_test_success_sets_active** — Successful connection test should set `is_active=True` and update last_health_check
239. **test_create_encrypts_credentials** — `create` should encrypt username and password before storing
240. **test_get_by_id_tenant_isolation** — `get_by_id` for datasource in different tenant should return None

## analytics_service.py (app/services/analytics_service.py)

241. **test_track_event_unknown_event_name** — `track_event` with unknown event name should still store (no validation against ALL_EVENTS)
242. **test_track_event_with_complex_data** — `track_event` with nested dict in event_data should serialize to JSON
243. **test_get_user_events_limit_zero** — `get_user_events` with `limit=0` should return empty list
244. **test_get_user_events_limit_applied** — `get_user_events` with `limit=2` should return at most 2 events
245. **test_track_event_non_serializable_data** — `track_event` with non-JSON-serializable data (e.g., datetime) should handle via json.dumps

## audit_service.py (app/services/audit_service.py)

246. **test_log_action_creates_audit_log** — `log_action` should create an AuditLog with all provided fields
247. **test_log_action_does_not_commit** — `log_action` should NOT commit (caller must commit)
248. **test_log_action_empty_optional_fields** — `log_action` with empty resource_id and details should work
249. **test_log_action_very_long_details** — `log_action` with very long details string should work
250. **test_log_action_special_chars_in_details** — `log_action` with SQL-like content in details should store as-is
251. **test_log_action_unicode_fields** — `log_action` with Unicode in action, resource_type, details should work

## email_service.py (app/services/email_service.py)

252. **test_send_email_smtp_not_configured** — When `settings.smtp_host` is empty, `send_email` should return `False` without error
253. **test_send_email_smtp_failure** — When SMTP connection fails, `send_email` should return `False` (catches exception)
254. **test_send_verification_email_contains_token** — Verification email HTML should contain the verification token in the link
255. **test_send_verification_email_contains_link** — Verification email HTML should contain `_get_frontend_url()` base URL
256. **test_send_password_reset_email_contains_token** — Reset email HTML should contain the reset token in the link
257. **test_send_password_reset_email_contains_link** — Reset email HTML should contain `_get_frontend_url()` base URL
258. **test_send_email_subject_encoding** — Email with Unicode subject should be properly encoded (UTF-8)
259. **test_send_email_to_unicode_address** — Email to address with Unicode (IDN) should work
260. **test_get_frontend_url** — `_get_frontend_url()` should return `settings.frontend_url`

## rag_schema_service.py (app/services/rag_schema_service.py)

261. **test_extract_keywords_only_english** — Question with only English words should return them as keywords
262. **test_extract_keywords_mixed_chinese_english** — Mixed Chinese/English question should extract both types
263. **test_extract_keywords_punctuation_removed** — Question with punctuation should have punctuation removed from keywords
264. **test_find_relevant_tables_model_without_name** — Model without "name" key should not crash (uses `.get("name", "")`)
265. **test_find_relevant_tables_model_without_columns** — Model without "columns" key should not crash (uses `.get("columns", [])`)
266. **test_find_relevant_tables_relationships_preserved** — Relevant table should include relationships
267. **test_format_schema_context_table_without_description** — Table without description should format correctly (no "说明:" line)
268. **test_format_schema_context_table_empty_columns** — Table with empty columns list should format with just "字段:" and nothing under it
269. **test_format_schema_context_table_with_relationships** — Table with relationships should include "关联:" section
270. **test_format_schema_context_column_with_all_attrs** — Column with nullable=True, primary=True, and comment should show all attributes
271. **test_get_rag_schema_empty_json_object** — `get_rag_schema("q", "{}")` should return empty string (no models in empty JSON)
272. **test_get_rag_schema_with_max_tables_limit** — `get_rag_schema` with many matching tables should respect `max_tables` limit

## auth.py schemas (app/schemas/auth.py)

273. **test_register_password_exactly_8_accepted** — Password of exactly 8 chars with letter+digit should pass validation
274. **test_register_password_exactly_7_rejected** — Password of 7 chars should fail (ValueError)
275. **test_register_password_only_special_chars_rejected** — `"!@#$%^&*"` should fail (no letter, no digit)
276. **test_register_password_very_long** — Very long password (1000+ chars) should work
277. **test_register_password_unicode_letters** — Password with Unicode letters (if regex `[a-zA-Z]` only matches ASCII) should be tested
278. **test_password_reset_confirm_same_validators** — `PasswordResetConfirm.new_password` should have same validation as `RegisterRequest.password`
279. **test_login_request_no_validation** — LoginRequest should not have password validation (only RegisterRequest does)
280. **test_refresh_request_empty_token** — RefreshRequest with empty string token should be accepted by Pydantic (validated at endpoint level)
281. **test_email_verify_request_empty_token** — EmailVerifyRequest with empty string token should be accepted by Pydantic (validated at endpoint)

## datasource.py schemas (app/schemas/datasource.py)

282. **test_datasource_create_whitespace_name** — Name with only spaces should be rejected by `name_not_empty` validator
283. **test_datasource_create_name_100_chars** — Name of exactly 100 chars should be accepted
284. **test_datasource_create_name_101_chars** — Name of 101 chars should be rejected
285. **test_datasource_create_unsupported_type_oracle** — `type="oracle"` should be rejected
286. **test_datasource_create_unsupported_type_mongodb** — `type="mongodb"` should be rejected
287. **test_datasource_create_sqlite_type_rejected** — `type="sqlite"` should be rejected (not in supported list)
288. **test_datasource_create_port_zero** — `port=0` should be accepted by schema (validation happens at DB level)
289. **test_datasource_create_port_max** — `port=65535` should be accepted
290. **test_datasource_create_empty_host** — `host=""` should be accepted by schema (validated at connection time)
291. **test_datasource_create_empty_database_name** — `database_name=""` should be accepted by schema
292. **test_datasource_create_extra_params_default** — `extra_params` should default to `{}`
293. **test_datasource_update_all_fields_optional** — All fields in DataSourceUpdate should be optional (None by default)
294. **test_datasource_response_defaults** — DataSourceResponse with default values should serialize correctly

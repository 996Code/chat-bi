# ChatBI - 项目状态

## 当前阶段

**Phase 1-2 已完成，Phase 3 基本完成（88%，仅 Oracle 未做）**，Phase 4 开始中（22%）。

## 项目概览

**核心价值**：让不会 SQL 的人也能自助完成数据查询和可视化，3 秒内得到图表答案
**技术栈**：FastAPI + LangGraph + Vue3 + ECharts + MySQL + Redis
**当前 LLM**：Mimo API（mimo-v2.5-pro，token-plan-cn.xiaomimimo.com）

## Phase 进度

| Phase | 名称 | 状态 | 需求完成 | 进度 |
|-------|------|------|---------|------|
| 1 | 基础设施 | ✅ Done | 25/25 | 100% |
| 2 | 核心链路完善 | ✅ Done | 21/21 | 100% |
| 3 | 优化增强 | ✅ Done（基本） | 15/17（不含取消项） | 88% |
| 4 | 看板与 SaaS 化 | 🔨 In Progress | 5/9 | 56% |

## 需求覆盖率

- v1 总需求：88 个
- Phase 1+2 已完成：52 ✅
- Phase 3+4 已完成：22 ✅（含看板 CRUD + Widget SQL 保存/刷新 + 异步查询 pipeline_trace + 管线优化 + 看板分享 + 连接池优化）
- Phase 3+4 未完成：8 ❌（含 3 个已取消 + 1 个已回退）
- **总完成率：74/88 (84%)**

### 各模块完成情况

| 模块 | 总数 | 完成 | 未完成 |
|------|------|------|--------|
| AUTH | 8 | 8 | 0 |
| TENANT | 3 | 3 | 0 |
| DS | 7 | 5 | 2 |
| DSO | 10 | 10 | 0 |
| META | 9 | 7 | 2 |
| SQL | 10 | 10 | 0 |
| CHART | 9 | 8 | 1 |
| PERF | 6 | 4 | 2 |
| UX | 11 | 7 | 4（含 2 个已取消） |
| API | 5 | 5 | 0 |
| OPS | 6 | 3 | 3 |
| SEC | 4 | 4 | 0 |

## 测试状态

- **单元测试**：242 passed, 0 failed（排除 test_sql_quality.py 集成测试）
- **真实数据测试**：全部通过
  - 登录 + JWT 含 role ✅
  - 审计日志（admin 访问、analyst 403 拦截、多维度筛选）✅
  - 查询历史（搜索、状态筛选、自动保存元数据）✅
  - 数据源启停（admin toggle、analyst 403 拦截）✅
  - 自然语言查询（各城市订单数量 → 30 行数据 → bar 图表）✅
  - SSE 流式查询（intent/semantics/sql/data/chart/complete 6 事件）✅
  - SQL 解释 ✅
  - Refresh token 保留 role ✅
  - CSV 导出 ✅
  - Excel 导出（数据表+图表截图嵌入）✅
  - 多轮对话上下文 ✅
  - Pipeline 执行追踪 ✅
  - Pipeline Trace 增强：每步耗时 + 选中表/字段 + SQL 验证修复 + 自愈错误码 ✅
- **复杂查询**：15/15 passed
- **SSE 流式**：7 种事件类型全部通过
- **API 集成**：login, query, explain, save, cursor, masking 全部通过
- **Partial 补全测试**：13 个新测试覆盖 5 个需求

## 已补全的 Partial 需求

| 需求 ID | 名称 | 补全内容 |
|---------|------|---------|
| TENANT-01 | 多租户隔离 | execution.py/schema_selection.py/analytics.py/saved_query.py 全链路 tenant_id 过滤 |
| DSO-08 | 数据源启停 | is_active 检查 + POST /{id}/toggle 端点 + admin 权限守卫 |
| SEC-01 | 审计日志 | 结构化字段(sql_text/result_count/execution_time_ms/error_message) + 多维度筛选 + stream/explain 审计 |
| PERF-04 | 查询历史 | 自动保存查询历史 + 执行元数据 + 搜索/筛选(text/datasource/status) |
| TENANT-03 | RBAC | JWT 含 role + require_role 依赖 + admin/user/read_only 角色约束 + analytics sub→user_id bug 修复 |

## 本次修复的 Bug

| Bug | 根因 | 修复 |
|-----|------|------|
| Refresh token 丢失 role | auth.py refresh 端点 token_data 未包含 role | 添加 `"role": payload.get("role", "user")` |
| Datasource toggle 权限绕过 | 手动调用 `require_role("admin")(user)` 非 FastAPI 依赖 | 改为 `admin: dict = Depends(require_role("admin"))` |
| CSV 导出 500 错误 | query.py 引用不存在的 `csv_export` 服务 | 重写为内联 CSV 生成 |
| Redis pipeline 未 await | rate_limiter.py `pipe.execute()` 缺少 await | 改为 `await pipe.execute()` |
| Alembic 迁移 MySQL 不兼容 | `sa.String()` 无长度限制 | 改为 `sa.String(5000)` / `sa.Text()` |
| 对话时间戳显示"8小时之前" | 后端返回 naive datetime 无时区信息 | ISO 8601 格式 + Z 后缀 |
| 对话消息双重编码 | chatStore 每次更新都追加而非替换 | 修复序列化逻辑 |
| 多轮对话上下文丢失 | SQL 生成未传入历史消息 | generation.py 接收 history 参数 |
| Model/Migration 类型不同步 | 模型用 String，迁移用 Text | 统一为 Text（长文本列） |

## 关键技术决策

- AI Framework: LangGraph（确定性工作流）
- Semantic Layer: MDL-style + LLM 语义解析
- SQL Dialect: SQLGlot AST 校验
- Multi-tenancy: Shared table + tenant_id（全链路强制过滤）
- RBAC: require_role() 依赖工厂，admin/user/read_only 三角色
- Frontend: Vue3 + ECharts + Element Plus
- LLM: Mimo API（mimo-v2.5-pro），enable_thinking=false
- 缓存: Redis 统一（移除本地缓存）

## 已知问题

1. `test_sql_quality.py` 需要 running server，非单元测试，async fixture 兼容性问题未修复
2. 元数据自动刷新（DSO-04）尚未实现

## 近期完成

- **教学级中文注释**：graph.py、pipeline_executor.py 添加完整的教学级中文注释，覆盖 LangGraph 核心概念、SSE 事件流、缓存策略等
- **LLM API 错误处理**：新增 `LLMAPIError` 异常类，认证失败时抛出明确错误而非静默降级
- 后台查询补全执行流程（pipeline_executor.py 共享执行器，后台/SSE 查询统一管线）
- 后台查询实时进度（Redis 存中间步骤 + 前端轮询重放 pipeline_trace）
- 缓存策略优化（启动清空 + 模型刷新按数据源清除 + Redis per-datasource 索引）
- 自动数据库迁移（async_queries 表增加 intent/pipeline_trace 列，SHOW COLUMNS + ALTER TABLE）
- 环境变量统一到根目录（前后端/构建/部署共用单一 .env）
- 前端统一 /chat-bi 路径前缀，上游 Nginx 只需一条转发规则
- PostgreSQL 数据源支持（aiomysql 替换 asyncmy + PG init 脚本）
- Docker 部署完善（Debian-slim 运行时 + 国内源 + deploy.sh 模板选择）
- 数据模型同步改为异步任务（Redis 任务状态 + 前端轮询进度条）
- AI 管线重构：RAG → LLM 三步交互（schema_selection + SQL 生成 + 自愈）
- SSE 流式前端完整接入（6 事件类型 + pipeline 步骤可视化）
- Excel 导出（ExcelJS + 图表截图嵌入，中文列名）
- 多轮对话上下文（generation.py 接收 history）
- Pipeline Trace 增强（每步耗时、选中表/字段、SQL 验证修复、自愈错误码）

## 技术升级待办（借鉴海泰 ChatBI）

> 详见 `doc/海泰ChatBI完整代码分析.md` 第九部分

### P0 — 立即可实施（1-2 周）

- [ ] **Skills 机制**：创建 `skills/` 目录，SQL 生成前读取 SKILL.md 注入提示词，业务规则外置到 Markdown 文件，支持热更新
- [ ] **结构化意图输出**：intent.py 改用 `with_structured_output(IntentOutput)`，增加 `action`（query/explain/chart）和 `normalized_question`（剥离可视化指令）字段
- [ ] **normalized_question**：意图识别后剥离"用折线图展示"等可视化措辞，将纯净业务问题传给 Schema 选择，提高检索准确率

### P1 — 中期规划（1-2 月）

- [ ] **向量检索指标库**：引入 Milvus 或 pgvector，预定义业务指标（名称、描述、SQL、公式），实现语义检索 + LLM 二次筛选
- [ ] **会话状态持久化**：引入 PostgreSQL Checkpointer，替代内存状态管理，支持会话恢复和跨轮次状态传递
- [ ] **追问补全逻辑**：完善 intent_history 存储，实现维度继承 + 相对时间解析，改善多轮对话体验
- [ ] **LLM 生成图表**：将 `infer_chart_type` 规则推断替换为 LLM 生成完整 ECharts option，配合 chart Skills 约束样式

### P2 — 长期规划（3-6 月）

- [ ] **多 Agent 架构**：评估 deepagents 框架或自建 Leader-Worker 模式，统一状态管理
- [ ] **复合指标支持**：设计指标关联关系（factor_ie_ids），实现子指标自动展开，支持跨表聚合计算

## 下一步行动

1. P0 技术升级：Skills 机制 / 结构化意图输出 / normalized_question
2. Phase 4：Java 注解导出（4.5）— 低优先级，按需排期
3. Phase 3 遗留：Oracle 支持（3.1）— 视业务需求决定是否排期
4. ~~Phase 4：ClickHouse / CI/CD / 图表导出~~ — 已取消

---
*Last updated: 2026-05-10*